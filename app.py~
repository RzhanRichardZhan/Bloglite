from flask import Flask, render_template, request
import sqlite3, webapp
#let this be the main file

app = Flask(__name__)
conn = sqlite3.connect('test.db')
c = conn.cursor()



@app.route('/')
def index():
    return render_template("index.html")

